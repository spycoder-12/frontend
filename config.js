/*=====================================================
    FRONTEND CONFIG

    API_BASE points at your backend. Change this to your
    deployed backend URL when you go live (e.g.
    "https://api.yoursite.com") — for local development,
    the FastAPI default of http://127.0.0.1:8000 is used.

    NOTE: there is intentionally no admin key/password in
    this file. Admin login now happens through the
    /admin/login endpoint on the backend, which returns a
    short-lived session token — nothing secret ships in
    the JS bundle anymore.
=====================================================*/

const API_BASE = "https://backend-1-cj5p.onrender.com";